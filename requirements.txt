# --- 数据库驱动 (psycopg 3) ---
# 包含 binary (无需编译) 和 pool (连接池)
psycopg[binary,pool]==3.3.3

# --- Web 框架 ---
fastapi[standard]==0.131.0

# --- 开发与测试工具 (通常在本地或 CI 环境使用) ---
pytest==9.0.2
pytest-asyncio==1.3.0
httpx==0.28.1
